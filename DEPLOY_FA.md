# بالا آوردن محصول روی اینترنت — راهنمای کلیک‌به‌کلیک

این فایل برای کسی نوشته شده که نمی‌خواهد با ترمینال کار کند. همهٔ مراحل در
مرورگر انجام می‌شود.

---

## چرا GitHub Pages نه؟

GitHub Pages فقط فایل ثابت سرو می‌کند. این محصول سه سرویس دارد که باید
**اجرا** شوند: PostgreSQL، Redis و Odoo. هیچ‌کدام روی Pages اجراشدنی نیستند، پس
Pages در بهترین حالت یک پوستهٔ خالی نشان می‌دهد که چت کار نمی‌کند.

## چه چیزی deploy می‌شود

`Dockerfile` یک کانتینر می‌سازد که همه‌چیز داخلش است:

| سرویس | از کجا | پورت |
|---|---|---|
| nginx (تنها پورت بیرونی) | apt | `$PORT` |
| Odoo 18 | `github.com/odoo/odoo` برنچ `18.0` | ۱۸۰۶۹ (داخلی) |
| PostgreSQL + pgvector + pg_trgm | ویل `pgserver` | داخلی |
| Redis | ویل `redislite` | داخلی |
| فرانت‌اند React | `frontend/dist` (بیلد در ایمیج) | از طریق nginx |

`docker/entrypoint.sh` عملاً همان `sandbox/aibox_local.sh` را صدا می‌زند، پس
منطق نصب و seed همان چیزی است که قبلاً کامل تست شده؛ فقط بسته‌بندی کانتینر جدید است.

---

## گزینهٔ ۱: Render (رایگان، بدون کارت اعتباری)

1. برو به `https://dashboard.render.com`
2. اگر اولین بار است: **New +** → **Blueprint** → حساب GitHub را وصل کن →
   مخزن `aibox-odoo` را انتخاب کن. Render فایل `render.yaml` را خودش پیدا
   می‌کند و سرویس را می‌سازد. **Apply** را بزن.
3. صبر کن تا build سبز شود. **بار اول ۵ تا ۱۵ دقیقه طول می‌کشد** چون ۱۲۴ ماژول
   Odoo نصب می‌شود.
4. آدرس نهایی همان چیزی است که Render نشان می‌دهد:
   `https://aibox-xxxx.onrender.com`

**محدودیت پلن رایگان:** بعد از ~۱۵ دقیقه بی‌کاری خاموش می‌شود و درخواست بعدی
~۱ دقیقه طول می‌کشد تا بیدار شود. دیسک ماندگار هم ندارد، پس هر بار که از نو
بالا می‌آید دیتابیس دوباره ساخته می‌شود.

## گزینهٔ ۲: Railway (پایدارتر، هزینهٔ مصرفی)

1. برو به `https://railway.app/new`
2. **Deploy from GitHub Repo** → مخزن `aibox-odoo` را انتخاب کن.
3. Railway خودش `Dockerfile` را پیدا می‌کند و build می‌کند.
4. در تب **Settings** → **Networking** → **Generate Domain** را بزن تا آدرس
   عمومی بگیری.
5. برای ماندگاری دیتابیس: **Settings** → **Mount Volume** → مسیر
   `/data/pgdata`. بدون این، هر restart دیتابیس از نو ساخته می‌شود.

Railway بعد از اعتبار اولیهٔ رایگان، بر اساس مصرف حساب می‌کند.

---

## بعد از بالا آمدن

1. آدرس را باز کن. ورود با `demo.ceo@yourbrand.example` / `Demo1234!`
2. برو به **کنسول ادمین** → تب **«مدل زبانی»**
3. کلید API واقعی خودت را paste کن (OpenAI / Gemini / Groq / …) → **تست** →
   **تست و ذخیره**
4. از همان لحظه چت جواب واقعی می‌دهد. برای برگشت به حالت قبل، **حذف** را بزن.

می‌توانی به‌جای UI، متغیرهای محیطی را هم در داشبورد Render/Railway تنظیم کنی
(`AI_LLM_API_BASE`, `AI_LLM_API_KEY`, `AI_LLM_MODEL`, `AI_EMBEDDING_*`)؛ ولی UI
بدون redeploy کار می‌کند.

---

## آنچه تست شده و آنچه نشده

**تست‌شده:** منطق `sandbox/aibox_local.sh` به‌صورت کامل — نصب ۱۲۴ ماژول، seed
دادهٔ دمو، `/api/login`، `/api/chat`، و چرخهٔ کامل تب «مدل زبانی»
(ذخیره/تست/حذف) با ۳۱ تست پاس.

**تست‌نشده:** خودِ `docker build`. در محیطی که این فایل‌ها نوشته شد هیچ Docker
daemon ای در دسترس نبود (`docker: NOT AVAILABLE`)، پس **اولین build روی
Render/Railway اولین تست واقعی این بخش است.** اگر خطا داد، لاگ build را
کپی کن تا اصلاحش کنم.

## اگر build شکست خورد

لاگ را در داشبورد باز کن و این‌ها را نگاه کن:

| خطا | علت احتمالی |
|---|---|
| `npm ci` failed | `frontend/package-lock.json` با `package.json` هم‌خوان نیست |
| `pg_trgm` build failed | `build-essential` یا `make` در ایمیج نیست |
| health check timed out | نصب ماژول‌ها طولانی‌تر از حد مجاز پلن است |
| `AI_GATEWAY_ALLOWED_ORIGIN` | متغیر دامنهٔ پلتفرم شناخته نشده؛ دستی تنظیمش کن |
