# Disabled legacy memory provider

The TencentDB/MemoryCore experiment is intentionally not part of the production memory path.
The canonical provider is `company_ai_demo.models.memory_record.AiAgentMemoryRecord`.
These files are preserved for traceability and are not imported, installed, or executed.
