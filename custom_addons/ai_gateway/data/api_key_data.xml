<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">
    <!-- API keys cannot be created from XML data because ai.gateway.api.key
         stores only one-way hashes and the plaintext secret is returned only
         once by create_key(). Browser users authenticate through /api/login
         sessions; machine-to-machine keys are generated explicitly by
         provisioning scripts or administrators after installation. -->
</odoo>
