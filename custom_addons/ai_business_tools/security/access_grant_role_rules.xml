<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <!-- The "temporary access may target product roles only" whitelist is
       enforced in Python (ai.gateway.access.grant._check_target_role):
       ir.rule domains are evaluated at REQUEST time where ref() does not
       exist, so this check cannot live here in Odoo 18. -->
</odoo>
